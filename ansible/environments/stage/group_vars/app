#db_host: "{{ hostvars['reddit-db']['networkInterfaces'][0]['networkIP'] }}"
db_host: 10.166.0.37
nginx_sites:
  default:
    - listen 80
    - server_name "reddit"
    - location / {
        proxy_pass http://127.0.0.1:9292;
      }
